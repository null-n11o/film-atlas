# Task 1〜7の実装検証

2026-09-23。詳細設計と実装計画の承認を受け、Native方式で主担当が順に実装した。実コンテンツは未投入であり、最初の1作品の完成やサービス公開を意味しない。元の作業ツリーは7d33be8で変更なしだった。実装ブランチはcodex/film-atlas-implementation、作業先は.worktrees/implementation。

## 検証の範囲

Node.js 22.22.3、npm 10.9.8と計画で指定した依存関係を導入した。導入時のpeer警告はなく、npm監査結果は脆弱性0件だった。npm run check、npm test、npm run build、npm run validate:content、npm run test:e2eの最終結果を実行記録に保存した。型検査はエラー・警告0件、単体51件、E2E40件が成功。通常ビルドは空一覧・運営方針・404の3ページを生成した。原稿検証とローカル起動も成功した。

単体検証は形式、参照、出典、公開状態、Markdown、ネタバレ条件の継承、保存失敗、追加データの型、概略図、年代、成果物の検査を対象とする。隔離した一時プロジェクトで公開ページ・JSON・素材を生成した後、全対象を取り下げて再生成し、旧成果物の削除を確認した。検証失敗時に配信対象を残さないことも確認済み。

ブラウザー検証はChromium・WebKitで実施した。初期HTML、直接URL、再読み込み、他画面から戻る操作、通信完了前の解除、取得失敗と再試行、保存例外、未知の保存バージョン、JavaScript無効時を含む。遅延通信のテストはレスポンス完了後にも非表示を確認する。幅320・390・1280px、200%の文字拡大、スキップリンク、キーボード操作、44pxの地図操作領域、外部通信なしを検証した。WebKitのリンクへのキーボード移動はmacOSのOption+Tabを使用する。

## 画面確認

Browserスキルでローカルのテスト成果物をChromeに表示した。作品一覧、作品詳細、背景解説を1280pxと390pxで撮影し、文字、余白、折返しを確認した。導入の重複案内を削除して余白を縮め、背景画面の長い操作名を作品名と操作に分けた。出典一覧の番号の重複も解消した。Tabから本文への移動、地図のEnterによる選択、出典の位置への移動と戻る操作を手動確認した。200%文字拡大のスクリーンショットも目視確認した。

ブラウザー自体の200%ズームはショートカットで倍率変更を確認できなかったため、手動確認済みとは扱わない。自動テストの200%文字拡大と幅640pxでの再配置の確認を区別する。

画像はすべて架空のテスト資料を使う。実在作品の内容確認や完成データの証拠にはしない。

- [一覧・PC](screenshots/list-desktop.png)
- [一覧・スマートフォン](screenshots/list-mobile.png)
- [作品・PC](screenshots/work-desktop.png)
- [作品・スマートフォン](screenshots/work-mobile.png)
- [背景・PC](screenshots/background-desktop.png)
- [背景・スマートフォン](screenshots/background-mobile.png)

## 実行上の判断

- CodexのSuperpowers 6.3.0のリンク先がなかったため、インストール済み6.4.1のexecuting-plansを使用した。ハーネスは変更していない。
- 可逆的な実装判断の承認に基づいて専用worktreeを作成した。元の追跡ファイルへ実装を混ぜていない。
- Astro 7のpreviewはエージェントを検出すると自動でバックグラウンド起動する。Playwrightがプロセスを管理できるよう--ignore-lockで前景実行した。[公式CLI資料](https://docs.astro.build/en/reference/cli-reference/)。
- Astroがアンダースコアで始まるページディレクトリを除外するため、計画の_contentディレクトリを動的ルート[content]に置き換えた。生成URLは設計どおり/_content/を維持する。
- 公開用とテスト用の出力を分離する。通常ビルドはcontentだけを読み、テスト成果物は.runtime/e2e-distに生成する。静的JSONの配信ヘッダーはホスティング決定後の確認事項とする。

## 残る作業

対象作品、最終本数、人による内容確認担当は未確定。Task 8の実原稿制作に入る前に確認する。Task 9は最初の1作品の受入後に進める。会社側の企画正本と未決事項は指定パスに見つからず、未確認を維持する。Notionへの進捗書込は行っていない。

独立レビューの結果と対応は別途記録する。マージ、サービス公開、投稿、支払いは実施しない。
