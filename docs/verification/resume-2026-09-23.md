# 開発再開とローカル表示の確認

2026-09-23。PR #1のマージ後、元のリポジトリ `/Users/nakanokentaro/02_dev/film-atlas` で依存導入と画面確認を実施した。実コンテンツの制作は未着手。

## Gitと継続記録

[PR #1](https://github.com/null-n11o/film-atlas/pull/1)は同日19:45:51 JSTにマージ済み。再開時のmainはマージコミット6307ca6で、未コミット変更はなかった。fetch後のfast-forward確認でも更新は不要だった。

旧worktree `.worktrees/implementation` は235b17cで変更なし。mainに未反映のコミットはなかったが、`.superpowers/sdd/2026-09-23-film-atlas/progress.md` と検証ログが残っている。継続台帳を読んで保管を継続した。別作業用の `orca/workspaces/film-atlas/sablefish` も削除・変更していない。

今回の修正は元のリポジトリの `codex/resume-local-preview` で実施した。

## 起動方法

Node.js 22.22.3、npm 10.9.8で `npm ci` を実行した。依存316件の導入が成功し、監査結果は脆弱性0件だった。

通常の原稿を表示する場合はリポジトリ直下で `npm run dev` を実行する。実原稿が未投入のため作品一覧は空になる。起動時に表示されるLocalのURLを使う。確認時のポート4321と4322は使用中だったため、今回の起動先は `http://127.0.0.1:4323/` となった。

架空資料で画面を確認する場合は、通常の開発サーバーを停止して次の順に実行する。

```sh
npm run build:e2e
npx astro preview --ignore-lock --outDir .runtime/e2e-dist --host 127.0.0.1 --port 4324
```

確認用URLは `http://127.0.0.1:4324/`。画面上部にテスト専用であることを表示する。再生成しない限りコード変更はpreviewに反映されない。確認用ビルドと通常起動は `.runtime/build` を共有するため、ビルド完了後に通常の開発サーバーを起動する。テスト成果物を外部公開に使わない。

## 修正と検証

スマートフォンの作品一覧で「02」が2行に分かれる現象を確認した。番号欄の28pxに対して文字幅が約28.5pxあり、本文から継承した折返し設定が適用されていた。番号の改行を禁止し、モバイルの番号欄を文字幅に応じて広げた。修正後のDOM計測では「02」が1行になり、320pxでも横はみ出しはなかった。

Browserで作品一覧、作品詳細、背景解説を1280pxと390pxで目視確認した。修正後の一覧は320pxでも確認した。配色、書体、読む順序は維持している。実ブラウザーの200%ズームは今回も確認済みとしない。

再開時に型検査（エラー・警告・ヒント0件）、単体62件、E2E48件、原稿検証、通常ビルドが成功した。CSS修正後はE2E48件と通常ビルドを再実行して成功した。通常ビルドは空一覧・運営方針・404の3ページを生成した。

再検証時は別の開発プロセスが4322を使用していたため、Git対象外の一時設定 `.runtime/resume-playwright.config.ts` でbaseURLとテストサーバーを4325に変更した。最初の実行では外部通信テストが4322を固定参照しており、ローカル通信を外部通信と誤判定した。判定対象を設定のbaseURLのoriginへ変更し、Chromium・WebKitの両方で成功した。既定のテスト設定は変更していない。最終コマンドは `npx playwright test --config .runtime/resume-playwright.config.ts`。

## 次の作業

Task 8は最初の対象作品と公開年、初期版の掲載本数、人による内容確認担当の回答待ち。最初の1作品を完成・受入後にTask 9へ進む。架空資料を完成原稿として扱わない。

前回未確認だった会社側の[企画正本](/Users/nakanokentaro/01_kcp/workspace/product/ideas/2026-09-23-作品の背景地図/00-brief.md)と[未決事項](/Users/nakanokentaro/01_kcp/workspace/product/ideas/2026-09-23-作品の背景地図/05-open-questions.md)を今回は読めた。作品理解の優先、説明と考察の区別、出典確認の方針は承認済み設計と一致する。会社側の開発段階の記載は以前の状態を示しているため、今回の再開指示とリポジトリの承認・検証記録に従う。会社側の文書とNotionは更新していない。
