# film-atlasの画面方針

2026-09-23。承認済みの機能と通常CSSを維持し、映画の背景を読むための画面を設計する。共通スキルdesign-taste-frontendの編集誌向け指針と01_kcp/rules/writing.mdを適用した。design-consultationも所在を確認したが、今回は補助スキルだけで方針を決める。ui-ux-pro-maxのSKILL.mdは会社側の指定ディレクトリに存在しない。

## 配色と文字組み

背景は#f8f9f6、本文は#202923、補助文字は#59635b、アクセントは#275943、境界は#c9d0c8。リンクに下線を付ける。状態と記述の分類は色だけに依存せず、名称を併記する。外部フォントを取得せず、見出しは端末の日本語明朝体、本文と操作は日本語ゴシック体を使う。明朝体は作品の題名と見出しに限定し、資料を読む画面として本文との階層を示す。

本文は18px、行高1.95、最大38rem。見出しは端末幅に応じて28〜52px。出典と補助文字も14pxを下回らない。文字拡大時に固定高で切らない。DESIGN_VARIANCE=5、MOTION_INTENSITY=1、VISUAL_DENSITY=3とし、装飾的な動きは加えない。

## 余白と情報階層

余白は8・16・24・32・48・72pxを基本にする。本文の区切りは罫線と見出しで示し、すべての段落をカードにしない。PCは本文と補助案内の余白を確保し、一覧は番号・公開年・題名・概要の順で読む。作品詳細は安全な概要、ネタバレ操作、背景と人物、場所、年表、出典の順に置く。背景解説は背景の説明と作品との接点を分け、出典を各記述からたどれるようにする。

## 共通部品

ヘッダー、パンくず、見出し、記述ラベル、出典リンク、資料一覧、関連リンク、ネタバレ操作、場所一覧、年表を共通化する。歴史・背景、作中の描写、本人の発言、編集者の解釈は別のラベルを持つ。関係の種類と理由も表示する。出典には参照日、該当箇所、著者、書誌またはURLを示す。

ボタンは最小44px。フォーカスは3pxの輪郭で示す。許可操作は本文の更新領域外に置き、取得中・取得失敗・再試行・保存失敗を文章で知らせる。ポスター、写真、外部地図素材を必要条件にしない。

## モバイルと確認

768px未満では1列とし、左右余白を20px、320pxでは16pxにする。長いURL・題名は折り返す。地図の文章一覧を常時表示し、キーボードだけでも解説へ進める。320・390・1280pxと200%拡大を検証する。作品一覧、作品詳細、背景解説のPC・スマートフォン画面をブラウザーで撮影し、文字、余白、折返し、操作状態を確認する。テスト用画面は架空データと明示し、通常ビルドには含めない。
