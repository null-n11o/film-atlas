---
import {renderPlaceMap} from '../lib/view/exploration';
import type {PageView} from '../lib/view/types';
interface Props {view:PageView}
---
<Fragment set:html={renderPlaceMap(Astro.props.view)}/>
