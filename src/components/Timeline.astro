---
import {renderTimeline} from '../lib/view/exploration';
import type {PageView} from '../lib/view/types';
interface Props {view:PageView}
---
<Fragment set:html={renderTimeline(Astro.props.view)}/>
